$(document).ready(function(){
  $("#search-form").on("submit", function(e){
    e.preventDefault();
    var searchTerm = $("#search-input").val();
    // Use the searchTerm to filter the dinosaurs
  });
});
